name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read
  packages: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Android build
        uses: DrewAPicture/exposures-common/.github/actions/setup-android-build@2fe940dfc68c696a8507a104390d3a27aebabb76 # exposures-common@2fe940d (2026-08-18)

      - name: Run tests
        run: ./gradlew test
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Safety gate from exp-shared-library-feasibility-plan.md: phone and watch must pin the
      # same exposures-common version, since they share Data Layer DTOs/paths at runtime — a
      # mismatch here is exactly the drift this whole extraction exists to prevent.
      - name: Verify exposures-common version matches exposures-watch
        run: |
          this_version=$(grep '^exposuresCommon = ' gradle/libs.versions.toml | sed -E 's/.*"(.*)".*/\1/')
          other_version=$(curl -fsSL https://raw.githubusercontent.com/DrewAPicture/exposures-watch/main/gradle/libs.versions.toml | grep '^exposuresCommon = ' | sed -E 's/.*"(.*)".*/\1/')
          echo "phone pins exposures-common $this_version; watch pins $other_version"
          if [ "$this_version" != "$other_version" ]; then
            echo "::error::exposures-common version drift: phone=$this_version watch=$other_version"
            exit 1
          fi
