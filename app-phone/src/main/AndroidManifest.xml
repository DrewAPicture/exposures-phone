<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".ExposuresApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.Exposures">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Exposures">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Manifest-registered so the system can wake this up even when the app isn't running:
          MESSAGE_RECEIVED for the watch's capture-photo command, DATA_CHANGED so a fresh exposure
          sync from the watch is picked up in the background, not only while the app is foregrounded.
        -->
        <service
            android:name=".sync.WearMessageListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:path="/command/capture-photo" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:path="/exposures" />
            </intent-filter>
        </service>
    </application>
</manifest>
