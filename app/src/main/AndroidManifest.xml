<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:name=".ExposuresApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.Exposures">

        <!--
          Disabled in favor of explicit init in ExposuresApplication.onCreate() (Configuration.Provider)
          — the default androidx.startup-based initializer doesn't reliably run under Robolectric.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Exposures">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!--
          App Actions fulfillment target for "save exposure" voice capture (see the Google
          Assistant voice-capture plan and res/xml/shortcuts.xml). Custom App Actions intents
          fulfill only via a URL-template deep link, so this is a plain ACTION_VIEW/BROWSABLE
          activity, not a MAIN/LAUNCHER one — it's invisible and finishes itself.
        -->
        <activity
            android:name=".voice.VoiceCaptureActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="exposures.app"
                    android:pathPrefix="/voice/create" />
            </intent-filter>
        </activity>

        <!--
          No Preview/Activity UI — see CaptureForegroundService's doc comment. foregroundServiceType
          is required from Android 14 alongside the FOREGROUND_SERVICE_CAMERA permission above.
        -->
        <service
            android:name=".capture.CaptureForegroundService"
            android:exported="false"
            android:foregroundServiceType="camera" />

        <!--
          Manifest-registered so the system can wake this up even when the app isn't running:
          MESSAGE_RECEIVED for the watch's capture-photo/complete-roll commands, DATA_CHANGED so a
          fresh exposure sync from the watch is picked up in the background, not only while the
          app is foregrounded.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".sync.WearMessageListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:path="/command/capture-photo" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:path="/command/complete-roll" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:path="/command/request-rolls-sync" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:path="/command/connectivity-ping" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:path="/command/create-exposure-ack" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:path="/exposures" />
            </intent-filter>
        </service>
    </application>
</manifest>
